START

IMPORT:
  - time

DEFINE STRUCT Pasien:

  ATRIBUT:
    pasienID (primary key)
    nama (wajib)
    email (unik)
    password (wajib)
    nik (unik)
    tanggalLahir (tanggal)
    tempatLahir
    alamat (text)
    jenisKelamin
    noTelepon
    resetCode (opsional)
    resetCodeExpiry (opsional)
    createdAt (timestamp otomatis saat dibuat)
    updatedAt (timestamp otomatis saat diupdate)


FUNCTION TableName:

  RETURN "pasien" sebagai nama tabel di database

END
